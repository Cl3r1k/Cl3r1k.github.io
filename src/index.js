import './scss/main.scss';

const initApp = () => {
  // console.log('%c Completely loaded...', 'color: red;');
};

// eslint-disable-next-line no-undef
document.addEventListener('DOMContentLoaded', () => {
  initApp();
});
